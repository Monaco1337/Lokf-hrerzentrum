# Database
DATABASE_URL="file:./dev.db"

# App
APP_BASE_URL="http://localhost:3000"
NODE_ENV="development"

# CRM-Auth (Pflicht in Production)
# bcrypt-Hash (cost >= 12) erzeugen z.B. via: node -e "console.log(require('bcryptjs').hashSync('dein-passwort', 12))"
CRM_PASSWORD_HASH=""
# 32+ Byte random base64, z.B. via: node -e "console.log(require('crypto').randomBytes(48).toString('base64'))"
CRM_SESSION_SECRET=""

# Crypto-Salts
IP_SALT=""
TOKEN_PEPPER=""

# Magic-Link
MAGIC_LINK_TTL_MINUTES="60"

# Communication-Adapter (Magic-Link manuell)
COMMUNICATION_PROVIDER="mock"

# Automation providers (Lead welcome flow)
EMAIL_PROVIDER="mock"
WHATSAPP_PROVIDER="mock"
RESEND_API_KEY=""
EMAIL_FROM="Fairtrain <hallo@lokfuehrerzentrum.de>"
SMTP_HOST=""
SMTP_PORT="587"
SMTP_USER=""
SMTP_PASS=""
TWILIO_WHATSAPP_FROM=""

META_WABA_TOKEN=""
META_PHONE_NUMBER_ID=""
TWILIO_ACCOUNT_SID=""
TWILIO_AUTH_TOKEN=""
DIALOG360_API_KEY=""

# Cron (SLA-Sweep)
CRON_SECRET=""
